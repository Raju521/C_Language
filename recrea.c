#include<stdio.h>
main()
{
	int l,b, area;
	printf("the lenght of rec:");
	scanf("%d",&l);
	printf("the breath of rec:");
	scanf("%d",&b);
	area=l*b;
	printf("The area of recatngle%d",area);
	getch();
	
}
